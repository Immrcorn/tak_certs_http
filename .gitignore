# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# Local / site secrets (keep tracked config.env as the package defaults)
*.local
.env
.env.*

# Cert packages dropped into the serve directory (keep certs/.keep)
certs/*
!certs/.keep

# Generated QR handouts (recreate with ./create-qr.sh)
certs/download-qr.html
certs/download-qr.png
certs/download-qr.svg

# Local development / IDE (never ship to offline RHEL hosts)
.cursor/

# Local development
.cursor/